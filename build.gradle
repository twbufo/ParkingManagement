
  apply plugin: 'java'
  apply plugin: 'idea'

	test {
		testReport
	}
	
	sourceCompatibility = 1.6
  version = '1.0'

  jar {
  	archiveName 'parkingLotsModel.jar'
    manifest {
        attributes 'Implementation-Title': 'ParkingManagement', 'Implementation-Version': version
    }
  }

